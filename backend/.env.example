# Database — SQLite runtime (local + server)
DB_DIALECT=sqlite
DB_STORAGE=./database.sqlite

# RDS source — one-time migration only (npm run db:migrate:rds-to-sqlite)
# RDS_DB_HOST=
# RDS_DB_USER=
# RDS_DB_PORT=5432
# RDS_DB_DATABASE=
# RDS_DB_PASSWORD=
